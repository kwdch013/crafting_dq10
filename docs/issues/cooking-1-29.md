# Issue明細: 調理職人（#1-#29）

### #6 調理職人: 会心固定状態を管理する（2026-07-09 close済み）

現状:

- 食材ごとの固定状態を保持していません。
- 会心後も通常の現在値計算と同じ扱いです。

完了条件:

- 食材ごとに固定状態を保持できる。
- 固定状態の食材はダメージ計算対象から除外される。
- 固定状態がUIで分かる。

URL:

```text
https://github.com/kwdch013/crafting_dq10/issues/6
```

### #7 調理職人: ミラクルグリルの必殺処理を実装する

現状:

- ミラクルグリル専用の特技、必殺使用状態、miss判定を実装済みです。
- フライパン配置で対象食材を選び、ミラクルグリルを一度だけ適用できます。
- 理想値超過時は miss として現在値を変更しません。

完了条件:

- 理想値以下なら基準値ちょうど、理想値超過ならmissになる。
- 成功時に誤差0と固定状態が反映される。
- コンテナ上の単体テストで成功・miss・特技登録を確認する。

URL:

```text
https://github.com/kwdch013/crafting_dq10/issues/7
```

### #8 調理職人: 混ぜ合わせで会心固定を解除する

現状:

- 固定状態そのものが未実装です。
- 混ぜ合わせによる固定解除も未実装です。

完了条件:

- 混ぜ合わせを特技として扱える。
- 対象食材の固定状態を解除できる。
- 固定解除後のUI表示と判定が更新される。

URL:

```text
https://github.com/kwdch013/crafting_dq10/issues/8
```

### #9 調理職人: 特性のターン効果を計算へ反映する

現状:

- レシピごとの `traitId` は記録できます。
- 4ターンごとの光・戻り、光、回復状態は計算に反映していません。

完了条件:

- ターン数または周期状態を管理できる。
- 光・戻り、光、回復が計算へ反映される。
- 特性がUIで確認できる。

URL:

```text
https://github.com/kwdch013/crafting_dq10/issues/9
```

### #10 調理職人: 2マス食材をグループとして移動・判定する

現状:

- `ingredientGroupId` が同じマスは、フライパン配置で同時に移動できます。
- バトルステーキ、ヒールカルボナーラは画像参照からグループ情報を登録済みです。
- 未登録レシピの食材グループ情報は未整備です。

完了条件:

- レシピデータで食材グループを表現できる。
- 2マス食材の移動、入れ替えが崩れない。
- 既存の1マス食材レシピが従来通り動作する。
- 未登録レシピの食材グループ情報を追加する。

URL: https://github.com/kwdch013/crafting_dq10/issues/10

### #11 調理職人: レシピ特性の実データ割り当てを検証する
現状:

- UI選択肢は `光`、`光・戻り`、`回復` の3種類に整理済みです。
- 旧 `none` は `light`、旧 `return` は `recovery` として扱います。
- 全レシピの実特性は参照画像または実測で未検証のものがあります。

完了条件:

- 全調理レシピの特性が信頼できる参照または実測で確認される。
- 未確認レシピがある場合は暫定値であることを追跡できる。
- 調理職人メモとレシピデータ設計が更新される。

URL: https://github.com/kwdch013/crafting_dq10/issues/11

### #12 調理職人: 複数マス食材の方向入れ替えをブラウザ操作で検証する

現状:

- `app/board-layout.js` と `tests/board-layout.test.js` で方向入れ替えのロジック単体テストはあります。
- テンキー配置の `1,2` が肉、`3` が野菜のように、相手食材が2マス移動する入れ替えは不可にする必要があります。
- ブラウザ上のクリック操作、Undo/Redoを含むE2E検証は未整備です。

完了条件:

- 相手食材が2マス以上移動する方向入れ替えは不可になる。
- 盤面クリックで代表ケースを確認できる。
- 2マス+2マス、2マス+空白、相手も1マス移動で収まる2マス+1マスで座標表示が崩れない。
- Undo/Redo後も場所種別と盤面座標が一致する。

URL:

```text
https://github.com/kwdch013/crafting_dq10/issues/12
```

### #13 調理職人: 画面取得時に素材と初期位置の対応を判別できるようにする

現状:

- レシピ側には初期位置を記載しています。
- 画面から取得した状態では、どの素材がどの初期位置だったか判断できません。
- 初期位置情報を利用した検証、修正、データ登録時に対応関係を手作業で推測する必要があります。

対応結果:

- 盤面上の初期位置表示は不要とし、表示しません。
- 初期位置専用の内部状態とDOM属性は保持しません。
- 計算と移動には現在位置の `gridCell` を使用します。
- 関連ドキュメントには座標を表示しない方針を記載します。

URL:

```text
https://github.com/kwdch013/crafting_dq10/issues/13
```

### #16 調理職人: 食材画像の見切れと光効果オンオフを修正する

現状:

- 食材画像の見切れ、光効果の明示操作、Undo/Redo時の効果状態維持を確認します。

完了条件:

- 食材画像、現在値、判定基準、判定ステータスが重ならず、`光` と `光・戻り` をオン/オフできる。
- 効果なし状態が保存読込、再描画、Undo/Redo後も維持される。
URL: https://github.com/kwdch013/crafting_dq10/issues/16

### #21 調理職人: 配置移動後に推定食材画像が消える

現状:

- 配置移動時にレシピIDが `custom` になり、選択元レシピのカテゴリを参照できませんでした。
- `ingredientGroupLabel` が未入力の肉料理・魚料理では、カテゴリ推定ができず画像が消えていました。
- 選択元レシピのカテゴリ情報を状態に保持する修正は実装済みです。
- その後、カテゴリ単位の画像推定が過剰であることが分かったため、issue #23 でカテゴリ推定を停止します。

完了条件:

- レシピが手入力扱いになっても推定食材画像が維持される。
- 回帰テストでカテゴリ保持による画像推定を確認する。
- 関連ドキュメントを更新する。

URL:

```text
https://github.com/kwdch013/crafting_dq10/issues/21
```

### #23 調理職人: 初期表示の食材画像がカテゴリ推定で全て肉になる

現状:

- `ingredientGroupLabel` が未入力のマスで、レシピカテゴリ `meat-dishes` を根拠に全て肉画像として表示されます。
- カテゴリだけではマスごとの食材種別を断定できず、野菜、卵、麺などの可能性があります。
- カテゴリまたはレシピ名だけで画像を推定しない修正を実装済みです。

完了条件:

- カテゴリまたはレシピ名だけで食材画像を推定しない。
- 明示ラベルがある食材画像は従来通り表示される。
- 回帰テストと関連ドキュメントを更新する。

URL:

```text
https://github.com/kwdch013/crafting_dq10/issues/23
```

### #25 調理職人: ミラクルグリルの2マス食材適用と固定判定を追加する

現状:

- ミラクルグリルは選択した1マスだけに適用されていました。
- 固定状態は単に固定として扱われ、本会心固定か偽会心の可能性ありかを区別していませんでした。
- 肉または魚の2マス食材へ同時適用し、固定時の増加量から判定する修正を実装済みです。

完了条件:

- 2マス肉/魚の両マスにミラクルグリルが適用される。
- 本会心固定と偽会心の可能性ありを判定・表示できる。
- 回帰テストと関連ドキュメントを更新する。

URL:

```text
https://github.com/kwdch013/crafting_dq10/issues/25
```

### #29 調理職人: バトルパッツァのレシピデータを追加する

現状:

- `バトルパッツァ` は `api/data/crafts/cooking/recipes.json` とフロントフォールバックへ登録済みです。
- 参照画像とユーザー提供値をもとに、魚2組を `140-170`、野菜2マスを `160-190` として反映しました。

完了条件:

- 表示プリセットのテストと関連ドキュメントまで更新済みです。

URL:

```text
https://github.com/kwdch013/crafting_dq10/issues/29
```
