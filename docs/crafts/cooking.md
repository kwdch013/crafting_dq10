# 調理職人メモ

調理職人に固有の会心、固定、特殊状況、食材サイズの仕様メモです。

このファイルは、今後の計算エンジン、画面認識、レシピデータ整備で参照する前提知識をまとめます。

## 実装状況

| 項目 | 状況 |
| --- | --- |
| 位置別ダメージ表示 | 実装済み |
| 食材別基準幅表示 | 実装済み |
| レシピ特殊状況の記録 | 実装済み |
| フライパン配置入れ替え | 実装済み |
| 上へずらす操作 | 実装済み |
| 会心固定状態の管理 | 未実装 |
| ミラクルグリルの専用処理 | 未実装 |
| 2マス食材の同時移動 | 未実装 |
| 混ぜ合わせによる固定解除 | 未実装 |
| 特殊状況の計算反映 | 未実装 |

## 会心ダメージ

会心が出たとき、そのダメージは基本的に、その具材があるマスで出る通常ダメージの2倍になります。

例:

- 四隅の通常ダメージが `3 / 4 / 5`
- 会心ダメージは `6 / 8 / 10`

そのため、会心で出る最大ダメージは、そのマスで出る可能性がある通常ダメージ最大値の2倍までです。

## 会心停止

会心ダメージが理想値を超える場合、焼き具合は理想値で止まります。

このとき、その具材は理想値との誤差が `0` になり、ゲージが固定されます。

判定上の意味:

- 会心ダメージが奇数として観測された場合、通常ダメージの2倍では表せないため、理想値で止まった本会心と見なせます。
- そのマスの通常ダメージ最小値の2倍より小さい会心ダメージが観測された場合も、理想値で止まった本会心と見なせます。
- 会心で固定されたときに、観測ダメージがそのマスの通常ダメージ候補の2倍と一致する場合は、偽会心の可能性があります。

## 会心固定

会心が出たときにダメージが2ゲージ目に届く、または2ゲージ目の間で会心が出た場合、その具材は固定されます。

固定された具材には、それ以上ダメージが入りません。

例外:

- `混ぜ合わせ` を使うと、固定状態を解除できます。

今後の実装では、食材ごとに固定状態を保持する必要があります。

候補フィールド:

```json
{
  "locked": true
}
```

## 超過中の会心

現在の焼き具合が理想値を超えている場合、会心の焼き具合は出ません。

理想値を超えた状態で `ミラクルグリル` を使うと、miss扱いになります。

## ミラクルグリル

`ミラクルグリル` は調理職人の必殺です。

職人中に確率で一回発動でき、対象食材が理想値を超過していなければ、確定で会心扱いになり、基準値ちょうどになります。

実装時の判定:

| 状態 | 結果 |
| --- | --- |
| 現在値が理想値以下 | 基準値ちょうど、誤差0、固定 |
| 現在値が理想値超過 | miss |

## 特殊状況

レシピごとに、4ターンに一回発生する特殊状況があります。

現在の分類:

| ID | 表示名 | 内容 |
| --- | --- | --- |
| `none` | 通常 | 4ターンごとの特殊状況なし |
| `recovery` | 回復 | 超過しているもの、または基準値外にあるもののうち、最も値が大きい食材が1つ回復 |
| `double-half` | 倍半 | 威力が倍になる状態と半分になる状態が交互に訪れる |
| `light-recovery` | 光・回復 | 上下左右が光る回と、四隅が回復する回が交互に訪れる |

レシピデータでは `specialEventId` に記録します。

例:

```json
{
  "name": "バトルステーキ",
  "specialEventId": "light-recovery"
}
```

`バトルパッツァ` は `光・回復` の例として扱いますが、現在のレシピデータには未登録です。追加時は `specialEventId: "light-recovery"` を設定します。

## 食材サイズ

食材には、1マス食材と2マス食材があります。

2マス食材は、2つのマスが同時に動く仕様です。

今後の実装では、同一食材を構成するマスをグループとして扱う必要があります。

候補フィールド:

```json
{
  "ingredientGroupId": "meat-1",
  "ingredientSize": 2
}
```

## 食材種別

肉、魚、卵、野菜などの食材種別と基準幅の対応は、別途共有された実データをもとに整備します。

現時点では、未確認の食材種別を断定しません。

## ダメージ表

四隅、上下左右、中央、強火焼き、弱火焼き、火力アップターンなどの詳細なダメージ表は、公式ガイドブックの「氷の領海＋職人の極意編」に掲載されている情報を参照対象とします。

このリポジトリでは、確認済みの値を `app/crafts/shared/cooking-damage.js` に反映します。
